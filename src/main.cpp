#include <iostream>

#include "Transducer.h"
#include "FileUtils.h"
#include "TransducerCreator.h"
#include "TestUtils.h"
#include "creator2.h"
#include "test.h"
#include <chrono>
#include <cstring>
#include <random> 
#include <algorithm> 
#include <cassert>

int main() {
    exam1();
    // myTest5();
    // myTest6();
    // test0();
    // const char* file = "/home/deyan/Desktop/min-subseq-transducer/test_simple/t1.csv";
    // auto dict = read_utf8_csv(file);

    // Transducer t;
    // std::map<StateSignature, State> minimized;
    // std::tie(t, minimized) = fromSortedDictionary(dict);

    // auto res = getLanguage(t);
    // for (auto p : res) {
    //     print_char32_vector(p.first);
    //     std::cout << "   " << p.second << std::endl;
    // }
    // std::cout << std::endl;

    // t.print();
    return 0;
}